# WebSafeScan

A non-exploitative Bash tool that checks HTTP security headers, cookie flags, allowed methods, and TLS configuration.
**Only run this tool against hosts you own or have explicit written permission to test.**

Usage:
\`\`\`
./detailed_webscan.sh https://example.com
\`\`\`
